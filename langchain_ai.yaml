name: langchain_ai
channels:
  - defaults
dependencies:
  - python=3.13
  - ca-certificates
  - openssl
  - pip>=24.0
  - pandoc
  - pip:
    - -r requirements.txt
prefix: /opt/anaconda3/envs/langchain_ai
