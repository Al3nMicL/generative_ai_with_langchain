# NOTE: Install PyTorch BEFORE this file to pick up the correct GPU/CPU wheel.
#   GPU (CUDA 12.x): uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CPU-only:        uv pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Then: uv pip install -r requirements.txt

# ── Core LangChain ecosystem ──────────────────────────────────────────────────
langchain>=1.3.11
langchain-core>=1.4.8
langchain-community>=0.4.2
langchain-text-splitters>=1.1.2
langsmith>=0.9.8
langgraph>=1.2.8

# ── LangChain integrations ────────────────────────────────────────────────────
langchain-openai>=1.3.3
langchain-anthropic>=1.4.8
langchain-google-genai>=4.2.7
langchain-google-vertexai>=3.2.4
langchain_huggingface>=1.2.2
langchain-ollama>=1.1.0
langchain_groq>=1.1.3
langchain-experimental>=0.4.2
langchain_chroma>=1.1.0
langchain_mistralai>=1.1.6

# ── Vector stores & retrieval ─────────────────────────────────────────────────
docarray==0.41.0
rank_bm25==0.2.2

# ── Data & ML ─────────────────────────────────────────────────────────────────
datasets>=5.0.0
huggingface-hub>=1.22.0

# ── Compute / serving ─────────────────────────────────────────────────────────
ray>=2.56.0

# ── Dev tools & UI ───────────────────────────────────────────────────────────
jupyter>=1.1.1
streamlit>=1.59.0
ruff>=0.15.20

# ── Utilities ─────────────────────────────────────────────────────────────────
arxiv>=4.0.0
duckduckgo_search>=8.1.1
python-dotenv>=1.1.1
wikipedia==1.4.0
